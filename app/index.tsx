import React from 'react';
import { Loading } from '../src/ui';

/** מסך פתיחה — הניתוב האמיתי נעשה ב-Gate שב-_layout */
export default function Index() {
  return <Loading label="פותח את HomeBase…" />;
}
