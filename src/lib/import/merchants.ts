import { normKey } from './shared';
import type { Kind } from '../types';

/**
 * מילון עסקים מובנה. הערכים הם **שמות הקטגוריות** שנזרעות לכל משק בית חדש
 * (ראה `seed_categories` ב-0003_functions.sql); ההתאמה לקטגוריה בפועל נעשית
 * לפי שם, כך שגם משק בית ששינה שמות או הוסיף קטגוריות ימשיך לעבוד.
 *
 * המפתחות נבדקים כתת-מחרוזת על התיאור המנורמל (`normKey`), ולכן צריכים להיות
 * קצרים וייחודיים. הרשימה נבנתה מנתונים אמיתיים של המשתמש + רשתות נפוצות.
 */
export const CATEGORY_BY_MERCHANT: Record<string, string[]> = {
  'סופר ומכולת': [
    'שופרסל', 'רמי לוי', 'ויקטורי', 'יינות ביתן', 'מגה בעיר', 'טיב טעם', 'אושר עד', 'יוחננוף',
    'סופר דוידי', 'סופר פארם', 'am pm', 'ampm', 'טיב טעים', 'קופיקס מרקט', 'מינמרקט', 'מיני מרקט',
    'שוק בכפר', 'חצי חינם', 'זול ובגדול', 'קשת טעמים', 'מכולת',
  ],
  'מסעדות וקפה': [
    'מקדונלד', 'בורגר', 'קפה', 'ארומה', 'לנדוור', 'קפית', 'רולדין', 'ביסטופ', 'שניצליה', 'תמנון',
    'כיף של בגט', 'כוורת', 'מעדניית', 'bbw', 'משקט', 'מש קר', 'פשפש', 'wolt', 'וולט', 'תן ביס',
    'מסעדה', 'פיצה', 'סושי', 'המבורגר', 'בראנץ', 'קייטרינג', 'בייקרי', 'מאפה', 'גלידה', 'שווארמה',
    'פלאפל', 'רובן',
  ],
  'תחבורה ודלק': [
    'פז', 'סונול', 'דלק', 'דור אלון', 'yellow', 'רב קו', 'רב-קו', 'רכבת ישראל', 'אגד',
    'מטרופולין', 'גט טקסי', 'gett', 'יאנגו', 'yango', 'שמשונים', 'תדלוק', 'חניון', 'פנגו',
    'סלופארק', 'עימגה',
  ],
  'חשבונות בית': [
    'חברת חשמל', 'בזק', 'הוט', 'yes', 'פרטנר', 'סלקום', 'פלאפון', 'גולן טלקום', 'רמי לוי תקשורת',
    'מי אשקלון', 'מי אביבים', 'מקורות', 'תאגיד המים', 'עירייה', 'ארנונה', 'גז', 'סופרגז', 'אמישראגז',
  ],
  'בריאות ותרופות': [
    'סופרפארם', 'ניו פארם', 'בי פארם', 'גוד פארם', 'כללית', 'מכבי', 'מאוחדת', 'לאומית', 'בית מרקחת',
    'רופא', 'מרפאה', 'שיניים',
  ],
  'ביגוד והנעלה': [
    'קסטרו', 'פוקס', 'zara', 'h&m', 'רנואר', 'גולף', 'אמריקן איגל', 'דלתא',
    'נעלי', 'טרמינל איקס', 'shein',
  ],
  'פנאי ובילויים': [
    'הטבות פיס', 'פיס פלוס', 'מועדון שלך', 'סינמה סיטי', 'יס פלאנט', 'לב סינמה', 'הבימה', 'סטימצקי',
    'צומת ספרים', 'ספורט', 'חדר כושר', 'הולמס פלייס', 'איקס פיט',
  ],
  'מנויים ודיגיטל': [
    'netflix', 'נטפליקס', 'spotify', 'ספוטיפיי', 'google', 'apple.com', 'itunes', 'microsoft',
    'openai', 'chatgpt', 'youtube', 'disney', 'amazon', 'צימר', 'איקלאוד', 'icloud',
  ],
  'ילדים וחינוך': ['גן ילדים', 'צהרון', 'בית ספר', 'חוג', 'מעון', 'ועד הורים', 'טיפת חלב'],
  'חיות מחמד': ['פטס', 'חיות', 'וטרינר', 'אולסטאר פט', 'זולו'],
  'מתנות ותרומות': ['מתנות', 'פרחים', 'תרומה', 'עמותת'],
  'ביטוח': ['הראל', 'מגדל', 'כלל ביטוח', 'הפניקס', 'מנורה מבטחים', 'ביטוח לאומי', 'איילון'],
  'שונות': ['דמי כרטיס', 'עמלה', 'עמלות', 'ש הפקות', 'פרינטר סנטר'],
};

/**
 * אפליקציות תשלום בין אנשים. הכסף עובר דרכן לכל מטרה, ולכן הן מקבלות קטגוריה
 * ייעודית ולא ניחוש לפי בית עסק.
 *
 * ⚠️ **אסור להשתמש ב-`\b` על עברית.** `\w` ב-JavaScript הוא ASCII בלבד (בלי דגל
 * `u`), ולכן `/\bביט\b/` לא מזהה `מביט` ולא `בביט` — כלומר שובר בשקט את
 * `זיכוי מביט`. התבנית כאן מתירה תחילית עברית (מ/ב/ל/ה/ו/ש/כ) ודורשת סוף מילה,
 * וכך `ביטוח לאומי` ו-`ביטול עסקה` נדחים כראוי. התבנית רצה על פלט `normKey`,
 * שכבר המיר מקפים לרווחים והוריד פיסוק.
 */
const MONEY_APP_PATTERNS = [
  /(^|[\s\u0590-\u05FF])ביט(?=$|\s)/,
  /(^|[\s\u0590-\u05FF])פייבוקס(?=$|\s)/,
  /\b(bit|paybox)\b/i,
];

/** העברה גנרית — הבנק לא מוסר למי שולם, ולכן אי אפשר לנחש ואי אפשר ללמוד. */
const GENERIC_TRANSFER_PATTERNS = ['העברה'];

/** הקטגוריה שכל ההעברות היוצאות דרך אפליקציות תשלום נכנסות אליה. */
export const MONEY_TRANSFER_CATEGORY = 'העברות כספים';

/** היעד לצד הנכנס — נזרע לכל משק בית מאז 0003. */
export const OTHER_INCOME_CATEGORY = 'הכנסה אחרת';

export function isMoneyAppTransfer(description: string): boolean {
  const key = normKey(description);
  return MONEY_APP_PATTERNS.some((re) => re.test(key));
}

export function isGenericTransfer(description: string): boolean {
  const key = normKey(description);
  return GENERIC_TRANSFER_PATTERNS.some((p) => key.includes(p));
}

/**
 * שורות שלא לומדים מהן כלל. ⚠️ `העברה מהחשבון` מופיעה בקובץ הבנק ארבע פעמים
 * עם תיאור, סוג פעולה ואסמכתא זהים לחלוטין — ובכל זאת אלה ארבעה תשלומים שונים.
 * כלל נלמד על התבנית הזאת היה דוחף את כולם לקטגוריה אחת.
 */
export function isPersonalTransfer(description: string): boolean {
  return isMoneyAppTransfer(description) || isGenericTransfer(description);
}

let index: [string, string][] | null = null;

function buildIndex(): [string, string][] {
  const pairs: [string, string][] = [];
  for (const [category, merchants] of Object.entries(CATEGORY_BY_MERCHANT)) {
    for (const m of merchants) pairs.push([normKey(m), category]);
  }
  // תבניות ארוכות קודם — "רמי לוי תקשורת" לפני "רמי לוי"
  return pairs.sort((a, b) => b[0].length - a[0].length);
}

/**
 * שם הקטגוריה המשוערת, או null אם לא זוהתה.
 *
 * ⚠️ בדיקת אפליקציות התשלום חייבת להיות **לפני** לולאת המילון ולא ערך בתוכה:
 * המילון ממוין לפי אורך, ולכן `פיצה` (4) היה מנצח את `ביט` (3) ו-
 * `זיכוי מביט מפיצה ערב סוף` היה נופל על «מסעדות וקפה».
 *
 * ⚠️ והיא חייבת להיות תלוית-כיוון: «העברות כספים» היא קטגוריית הוצאה, ולכן
 * החזרתה לשורת `זכות` הייתה מצמידה קטגוריית הוצאה לשורת הכנסה.
 */
export function guessCategoryName(description: string, kind: Kind): string | null {
  if (isMoneyAppTransfer(description)) {
    return kind === 'income' ? OTHER_INCOME_CATEGORY : MONEY_TRANSFER_CATEGORY;
  }
  if (isGenericTransfer(description)) return null;
  const key = normKey(description);
  if (!key) return null;
  index ??= buildIndex();
  for (const [pattern, category] of index) {
    if (pattern && key.includes(pattern)) return category;
  }
  return null;
}
